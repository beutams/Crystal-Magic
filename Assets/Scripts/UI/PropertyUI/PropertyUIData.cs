// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Right-click Prefab → Assets/Tools/Generate UIData to regenerate

using UnityEngine;
using CrystalMagic.Core;

public class PropertyUIData : UIData
{
    public UINode Speed;
    public UINode Speed_Name;
    public UINode Speed_Value;
    public UINode MaxHealth;
    public UINode MaxHealth_Name;
    public UINode MaxHealth_Value;
    public UINode HealthRegen;
    public UINode HealthRegen_Name;
    public UINode HealthRegen_Value;
    public UINode MaxMana;
    public UINode MaxMana_Name;
    public UINode MaxMana_Value;
    public UINode ManaRegen;
    public UINode ManaRegen_Name;
    public UINode ManaRegen_Value;
    public UINode AttackPower;
    public UINode AttackPower_Name;
    public UINode AttackPower_Value;
    public UINode ChantSpeed;
    public UINode ChantSpeed_Name;
    public UINode ChantSpeed_Value;
    public UINode Fire;
    public UINode Fire_Name;
    public UINode Fire_Value;
    public UINode Water;
    public UINode Water_Name;
    public UINode Water_Value;
    public UINode Lighting;
    public UINode Lighting_Name;
    public UINode Lighting_Value;
    public UINode Wind;
    public UINode Wind_Name;
    public UINode Wind_Value;
    public UINode SkillRange;
    public UINode SkillRange_Name;
    public UINode SkillRange_Value;

    public override void Bind(Transform root)
    {
        Speed = UINode.From(Find(root, "Speed"));
        Speed_Name = UINode.From(Find(root, "Speed/Name"));
        Speed_Value = UINode.From(Find(root, "Speed/Value"));
        MaxHealth = UINode.From(Find(root, "MaxHealth"));
        MaxHealth_Name = UINode.From(Find(root, "MaxHealth/Name"));
        MaxHealth_Value = UINode.From(Find(root, "MaxHealth/Value"));
        HealthRegen = UINode.From(Find(root, "HealthRegen"));
        HealthRegen_Name = UINode.From(Find(root, "HealthRegen/Name"));
        HealthRegen_Value = UINode.From(Find(root, "HealthRegen/Value"));
        MaxMana = UINode.From(Find(root, "MaxMana"));
        MaxMana_Name = UINode.From(Find(root, "MaxMana/Name"));
        MaxMana_Value = UINode.From(Find(root, "MaxMana/Value"));
        ManaRegen = UINode.From(Find(root, "ManaRegen"));
        ManaRegen_Name = UINode.From(Find(root, "ManaRegen/Name"));
        ManaRegen_Value = UINode.From(Find(root, "ManaRegen/Value"));
        AttackPower = UINode.From(Find(root, "AttackPower"));
        AttackPower_Name = UINode.From(Find(root, "AttackPower/Name"));
        AttackPower_Value = UINode.From(Find(root, "AttackPower/Value"));
        ChantSpeed = UINode.From(Find(root, "ChantSpeed"));
        ChantSpeed_Name = UINode.From(Find(root, "ChantSpeed/Name"));
        ChantSpeed_Value = UINode.From(Find(root, "ChantSpeed/Value"));
        Fire = UINode.From(Find(root, "Fire"));
        Fire_Name = UINode.From(Find(root, "Fire/Name"));
        Fire_Value = UINode.From(Find(root, "Fire/Value"));
        Water = UINode.From(Find(root, "Water"));
        Water_Name = UINode.From(Find(root, "Water/Name"));
        Water_Value = UINode.From(Find(root, "Water/Value"));
        Lighting = UINode.From(Find(root, "Lighting"));
        Lighting_Name = UINode.From(Find(root, "Lighting/Name"));
        Lighting_Value = UINode.From(Find(root, "Lighting/Value"));
        Wind = UINode.From(Find(root, "Wind"));
        Wind_Name = UINode.From(Find(root, "Wind/Name"));
        Wind_Value = UINode.From(Find(root, "Wind/Value"));
        SkillRange = UINode.From(Find(root, "SkillRange"));
        SkillRange_Name = UINode.From(Find(root, "SkillRange/Name"));
        SkillRange_Value = UINode.From(Find(root, "SkillRange/Value"));
    }
}
