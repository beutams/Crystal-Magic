CameraComponent
-内容
    主相机 切换主相机，切换时会刷新UIComponent
    跟随 每帧跟随PlayerTag，做平滑移动
    震动 
        每帧会根据距离和时间计算出所有震动的强度
        用强度通过柏林噪声生成一个具体的震动向量
        将所有向量叠加得到最终震动
-接口
    (Un)Register 注册相机
    AddShake 注册一条震动